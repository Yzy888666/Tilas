<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yzy.mapper.EmpMapper">

        <update id="update" parameterType="com.yzy.pojo.Emp">
            UPDATE emp
            <set>
                <if test="username != null and username != ''">username = #{username},</if>
                <if test="name != null and name != ''">name = #{name},</if>
                <if test="gender != null">gender = #{gender},</if>
                <if test="phone != null and phone != ''">phone = #{phone},</if>
                <if test="job != null and job != ''">job = #{job},</if>
                <if test="salary != null">salary = #{salary},</if>
                <if test="entryDate != null">entry_date = #{entryDate},</if>
                <if test="image != null and image != ''">image = #{image},</if>
                <if test="deptId != null">dept_id = #{deptId},</if>
                update_time = #{updateTime}
            </set>
            WHERE id = #{id}

    </update>

    <delete id="deleteByIds">
        DELETE FROM emp
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="list" resultType="com.yzy.pojo.Emp">
        SELECT emp.*, dept.name deptName
        FROM emp
        LEFT JOIN dept ON emp.dept_id = dept.id
        <where>
            <if test="name != null and name != ''">
                emp.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="gender != null">
                AND emp.gender = #{gender}
            </if>
            <if test="begin != null">
                AND emp.entry_date &gt;= #{begin}
            </if>
            <if test="end != null">
                AND emp.entry_date &lt;= #{end}
            </if>
        </where>
        ORDER BY emp.update_time DESC
    </select>
<!--    手动封装结果-->
    <resultMap id="EmpResultMap" type="com.yzy.pojo.Emp">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="gender" column="gender"/>
        <result property="phone" column="phone"/>
        <result property="job" column="job"/>
        <result property="salary" column="salary"/>
        <result property="image" column="image"/>
        <result property="entryDate" column="entry_date"/>
        <result property="deptId" column="dept_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="deptName" column="dept_name"/>

        <!-- 一对多关联映射：员工工作经历 -->
        <collection property="exprList" ofType="com.yzy.pojo.EmpExpr">
            <id property="id" column="ee_id"/>
            <result property="empId" column="ee_empid"/>
            <result property="begin" column="ee_begin"/>
            <result property="end" column="ee_end"/>
            <result property="company" column="ee_company"/>
            <result property="job" column="ee_job"/>
        </collection>
    </resultMap>

    <select id="getInfo" resultMap="EmpResultMap">
        SELECT
            e.*,
            d.name AS dept_name,
            ee.id AS ee_id,
            ee.emp_id AS ee_empid,
            ee.begin AS ee_begin,
            ee.end AS ee_end,
            ee.company AS ee_company,
            ee.job AS ee_job
        FROM emp e
                 LEFT JOIN dept d ON e.dept_id = d.id
                 LEFT JOIN emp_expr ee ON e.id = ee.emp_id
        WHERE e.id = #{id}
    </select>
    <!-- 统计员工职位人数 -->
    <select id="getEmpJobData" resultType="java.util.Map">
        SELECT
            (CASE WHEN job=1 THEN '班主任'
                  WHEN job=2 THEN '讲师'
                  WHEN job=3 THEN '学工主管'
                  WHEN job=4 THEN '教研主管'
                  WHEN job=5 THEN '咨询师'
                  ELSE '其他' END) pos,
            COUNT(*) num
        FROM emp
        GROUP BY job
        ORDER BY num
    </select>

    <!-- 统计员工性别人数 -->
    <select id="getEmpGenderData" resultType="java.util.Map">
        SELECT
            (CASE WHEN gender=1 THEN '男性员工'
                  WHEN gender=2 THEN '女性员工'
                  ELSE '其他' END) name,
            COUNT(*) value
        FROM emp
        GROUP BY gender
        ORDER BY value
    </select>


    <!--    <delete id="deleteEmpByIds">-->

<!--    </delete>-->

</mapper> 